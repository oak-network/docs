{"title":"Body","body":{"content":{"application/json":{"schema":{"allOf":[{"type":"object","description":"Base envelope delivered to merchant webhook endpoints. Every webhook\nrequest body has the shape `{ id, event, category, data }`; concrete\nevent schemas compose this base via `allOf` and refine `event` /\n`category` to their `const` values and `data` to a specific schema.\n\nEach delivery carries three headers:\n  CrowdSplit-Signature        `t={unix-ts},v1={hex-hmac-sha256}`\n  CrowdSplit-Timestamp        seconds since epoch\n  CrowdSplit-Notification-Id  unique per delivery (idempotency)\n\nMerchants MUST verify the HMAC of the raw body using the webhook\nsecret returned at registration, and SHOULD reject the delivery if\n`CrowdSplit-Timestamp` differs from the current time by more than\n5 minutes. Duplicate `CrowdSplit-Notification-Id` values indicate a\nretry of a previously dispatched delivery and should be deduplicated.\n","required":["id","event","category","data"],"properties":{"id":{"type":"string","format":"uuid","description":"Unique notification ID (stable across retry attempts)."},"event":{"type":"string","description":"Event name; specialized via `allOf` per operation (e.g. `payment.succeeded`)."},"category":{"type":"string","description":"Event category; specialized via `allOf` per operation (e.g. `payment_lifecycle`)."},"data":{"type":"object","description":"Event-specific payload; each operation refines this via `allOf` to a concrete schema."}},"title":"WebhookEnvelope"},{"type":"object","properties":{"event":{"const":"customer.updated"},"category":{"const":"provider_registration_lifecycle"},"data":{"type":"object","description":"Data payload for customer, KYC, and provider registration events","required":["id","status","created_at","updated_at"],"properties":{"id":{"type":"string","format":"uuid","description":"Customer unique ID"},"status":{"type":"string","description":"Registration status (lowercased)"},"provider":{"type":"string","description":"CrowdSplit-managed upstream provider identifier. Lowercased in every\nmerchant-facing payload; the internal enum keys are defined in `util/enum/PLATFORMS.js`.\n\nNote: merchant-facing `*.provider` fields receive a special\ntransform (`util/helper.js:314-326`) that emits `avenia` in place\nof `brla` in the lowercased form; consumers should treat the two\nas equivalent for now.\n","enum":["stripe","bridge","wallet_service","crowd_split"],"title":"ProviderName"},"target_role":{"type":"string","description":"Target role (lowercased)"},"provider_response":{"type":"object","nullable":true,"description":"Raw excerpt of the provider's registration response. Shape\nvaries by provider — do not rely on specific keys.\n","additionalProperties":true},"failure_reason":{"type":["string","null"]},"readiness":{"type":["string","null"],"description":"Provider readiness status"},"created_at":{"type":"string","format":"date-time"},"updated_at":{"type":"string","format":"date-time"}},"title":"WebhookProviderRegistrationData"}}}]}}}}}